# Continuity pass (2026-08) — nc-roomba

## Changes
- Redrew `img/app.svg` as single-ink `currentColor` robot-vac silhouette (brand palette stays in app UI CSS).

## Verify
- Nav + Settings section icon tint correctly in light/dark themes.
